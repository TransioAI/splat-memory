"""Compute pairwise spatial relations between scene objects."""


def compute_spatial_relations(objects):
    """Compute left_of, right_of, above, below, in_front_of, on_top_of, distance."""
    pass
